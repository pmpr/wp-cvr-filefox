{% if pmpr_apply_filters('has_footer', 1) %}
    <footer id="site_footer" class="position-relative bg-gray-300">
        {% include get_template('features', _self) %}
        <div class="container pt-5">
            {% include get_template('top', _self) %}
            <hr class="my-5 border-dark border-2">
            {% include get_template('main', _self) %}
        </div>
        {% include get_template('bottom', _self) %}
    </footer>

{% endif %}

{{ wp_footer()|raw }}
<div class="backdrop position-fixed o-50 vw-100 vh-100 bg-gray-900 top-0 left-0 z-sm"></div>
</body>
</html>

