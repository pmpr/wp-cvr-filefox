{% if items is not empty %}
    <nav class="mb-4">
        {{ render_if_has_content('h2', {class: 'h2 mb-3'}, title) }}
        {% include get_cover_template('common/labels') %}
    </nav>
{% endif %}

